const { add } = require('./index')
const { assert } = require('chai')

describe('string calculator', () => {
  describe('add', () => {
    it('placeholder', () => {
      add()
      assert(true)
    })
  })
})
